# Web / API
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9

# Data
SQLAlchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
tzdata>=2024.1           # zoneinfo 的 IANA 時區資料庫（Windows 必需）

# 登入 / AD 驗證
itsdangerous>=2.2        # SessionMiddleware 簽章
ldap3>=2.9               # AD（NTLM）驗證
pyasn1>=0.6.4            # ldap3 相依；<0.6.4 有三個 ASN.1 解碼 DoS CVE（CVE-2026-59884/5/6）
pycryptodome>=3.20       # MD4 相容修補（NTLM 需要）

# vSphere（真實模式才需要；模擬模式可不裝）
pyvmomi>=8.0
